<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Permiso de internet para conectar con el backend -->
    <uses-permission android:name="android.permission.INTERNET"/>
    <!-- Permiso para detectar si hay conexión de red -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.GetToDay"
        android:usesCleartextTraffic="true">
        <!--
            usesCleartextTraffic="true": permite conexiones HTTP (no cifradas).
            Necesario porque el servidor usa http:// en lugar de https://.
            Sin esto Android bloquea todas las peticiones al servidor.
        -->

        <activity
            android:name=".PerfilNegocioActivity"
            android:exported="false" />
        <activity
            android:name=".ServiciosNegocioActivity"
            android:exported="false" />
        <activity
            android:name=".HomeNegocioActivity"
            android:exported="false" />
        <activity
            android:name=".PerfilClienteActivity"
            android:exported="false" />
        <activity
            android:name=".NuevaReservaActivity"
            android:exported="false" />
        <activity
            android:name=".HomeClienteActivity"
            android:exported="false" />
        <activity
            android:name=".RegistroActivity"
            android:exported="false" />
        <activity
            android:name=".LoginActivity"
            android:exported="false"
            android:windowSoftInputMode="adjustResize"/>
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>

</manifest>