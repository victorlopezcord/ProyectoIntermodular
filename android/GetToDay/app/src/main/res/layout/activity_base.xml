<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="@color/fondo_pantalla">

    <!--
        LAYOUT BASE — Sin Toolbar
        ─────────────────────────────────────────
        Cada pantalla tiene su propio título dentro
        de su layout XML como TextView.

        ESTRUCTURA:
          1. FrameLayout  → contenido de cada pantalla
          2. BottomNav    → menú inferior
        ─────────────────────────────────────────
    -->

    <!-- ── CONTENIDO DE LA PANTALLA ──────────── -->
    <FrameLayout
        android:id="@+id/contenedor"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:background="@color/fondo_pantalla"/>

    <!-- ── MENÚ INFERIOR CLIENTE ─────────────── -->
    <com.google.android.material.bottomnavigation.BottomNavigationView
        android:id="@+id/menuCliente"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@color/blanco"
        app:menu="@menu/menu_cliente"
        app:labelVisibilityMode="labeled"
        app:itemTextColor="@color/azul_oscuro"
        app:itemIconTint="@color/azul_oscuro"
        android:visibility="gone"
        android:elevation="8dp"/>

    <!-- ── MENÚ INFERIOR NEGOCIO ─────────────── -->
    <com.google.android.material.bottomnavigation.BottomNavigationView
        android:id="@+id/menuNegocio"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@color/blanco"
        app:menu="@menu/menu_negocio"
        app:labelVisibilityMode="labeled"
        app:itemTextColor="@color/azul_oscuro"
        app:itemIconTint="@color/azul_oscuro"
        android:visibility="gone"
        android:elevation="8dp"/>

</LinearLayout>